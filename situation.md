# 项目现状诊断

> 诊断日期：2026-09-05

## 一句话结论

**简历优化器本身是好的（本地服务 + LLM 都正常），唯一坏的是 natapp 内网穿透隧道连不上认证服务器，导致公网地址失效。** 你「监控不了数据」和「没办法进行简历优化」，本质是同一个原因：**从公网打不开 Web UI**。

---

## 当前状态清单

| 组件 | 状态 | 证据 |
|------|------|------|
| Gradio Web 服务（本地 127.0.0.1:7860） | ✅ 正常运行 | 端口 7860 正在监听，HTTP 200 |
| SQLite 数据库（data/optimization.db） | ✅ 正常 | 文件存在，服务每次启动都成功初始化 |
| LLM API（api.deepseek.com） | ✅ 正常 | 实测 `deepseek-chat` 和 `deepseek-v4-pro` 都能正常返回 |
| natapp 隧道进程 | ⚠️ 进程活着但卡死 | 卡在「正在连接认证服务器进行登录…」，无任何出站连接 |
| natapp 公网地址 | ❌ 失效 | `g984466a.natappfree.cc` 已返回 `Tunnel not found` |

---

## 为什么「监控不了数据」和「没办法进行简历优化」

这两个问题**不是两个 bug，是同一个根因**：

- 「监控数据」= 打开 Web UI 里的 **Badcase 分析面板**（会话统计、四维平均分、错误分布、最近记录）。
- 「进行简历优化」= 点 Web UI 里的 **开始优化** 按钮。

这两件事都要求先**访问到 Web 界面**。而 Web 界面只绑定在 `127.0.0.1:7860`（本机回环地址），对外访问完全依赖 natapp 隧道把公网流量转发进来。

**隧道一断，公网就进不来 → 数据看不到，优化也点不了。**

> 关键点：本地服务其实一直是好的。如果你在**这台电脑上**直接访问 `http://127.0.0.1:7860`，监控数据和简历优化都是能用的。

---

## 根因分析：natapp 隧道为什么连不上

排查证据：

1. natapp 进程卡在登录阶段，日志始终是 `正在连接认证服务器进行登录...`，并且没有任何出站 TCP 连接。
2. natapp 的服务器域名（`server.natapp.cc`、`server.natapp.com`、`natapp.cc`）DNS 解析异常：
   - `server.natapp.cc` 的 A 记录返回 `240c::6666`（这是一个典型的 GFW DNS 污染地址）。
   - 直接 `nslookup` 这些域名全部超时。
3. 对比：普通网站正常（`baidu.com`、`api.deepseek.com` 都能通），说明**不是整机断网，而是 natapp 的海外服务器被墙/被污染**。
4. 系统代理（`127.0.0.1:26001`，已开启）也救不了：走代理访问 `server.natapp.cc` 依然不可达。

**结论**：这个 natapp 是国际版（`.natappfree.cc` 域名），其认证/隧道服务器在海外。国内网络下 DNS 被污染 + 服务器被墙，所以隧道时断时续、重连时直接卡死。之前能用（比如 `g984466a.natappfree.cc` 一度返回 200）只是短暂的侥幸连通，不稳定。

---

## 已验证的事实（测试证据）

- `http://127.0.0.1:7860/` → HTTP 200（本地服务可用）
- `https://api.deepseek.com` → 可达，`deepseek-chat` 返回正常（实际映射到 `deepseek-v4-flash`）
- `https://api.deepseek.com` → `deepseek-v4-pro` 返回正常（推理模型，带 `reasoning_content`）
- `.env` 里的 `MODEL_ID_THINKING=deepseek-v4-pro` 和 `thinking`/`reasoning_effort` 参数实测都被 DeepSeek 接受，**LLM 层没有问题**
- natapp 相关域名 DNS 解析失败 / 被污染

---

## 解决方案

### 短期（先让你能立刻用上）

1. **本机直接访问**：在跑服务的这台电脑上打开 `http://127.0.0.1:7860`，即可正常监控数据和跑优化（不依赖隧道）。
2. **局域网访问**（如果你和这台电脑在同一局域网）：把 `gradio_app.py` 里 `launch()` 的 `server_name="127.0.0.1"` 改成 `server_name="0.0.0.0"`，然后用局域网 IP 访问。

### 长期（要稳定公网访问）

natapp 国际版在国内不可靠，建议换成**国内可用的内网穿透**，二选一：

1. **cpolar**（国内服务，免费档稳定）：注册后拿 token，把本地 7860 端口映射出去，公网地址固定。
2. **natapp 国内版（natapp.cn）**：换用国内账号和 token，走国内服务器，域名稳定（需要实名认证）。

> 注意：natapp 免费隧道域名**每次重连都会轮换**，本身就不适合当固定入口。要固定地址必须绑定自己的域名或升级套餐。

---

## 待办 / 建议

- [ ] 决定用 cpolar 还是 natapp 国内版，替换现有的 `natapp.exe` + `config.ini`。
- [ ] 若需局域网/多机访问，把 `gradio_app.py` 的 `server_name` 改为 `0.0.0.0`。
- [ ] 更新 `start_server.bat` / `stop_server.bat`，去掉写死的旧公网域名。
