"""快速深度优化流水线 - 精简版 Prompt

Phase 0: JD能力映射 (会话级缓存，只跑一次)
Phase 1: 智能提取 (一步到位400-600字，替代原来的 Phase 1a+1b+2)
Phase 2: STAR合成 + HR评估 + 迭代 (最多3轮)
Phase 3: 编造审计 (输出后验证)
"""

FACT_CHECKER_PROMPT_VERSION = "fact_checker_v1.0"

# ============================================================
# Phase 0: JD 能力映射（会话级缓存）
# ============================================================

JD_ANALYZER_SYSTEM_PROMPT = """你是一位资深HR，擅长快速分析JD和优秀简历，提炼关键评估维度。

你的任务：阅读JD和优秀简历样本，提炼出该岗位最看重的通用产品能力和关键动作词。"""

JD_ANALYZER_TEMPLATE = """请分析以下材料，提炼岗位核心要求。

## 目标JD
{jd}

## 优秀简历参考
{reference_resumes}

## 输出要求（尽量精简）

### 关键动作词（5-10个）
从JD和优秀简历中提取最核心的动作动词，如：用户调研、数据分析、需求设计、项目管理、竞品分析等

### 需要的数据维度（5-8个）
该岗位最看重的量化指标类型，如：用户增长%、留存率、效率提升%、项目周期、团队规模等

### 优先级排序
按重要性排序前3的能力维度，用于指导经历提取的侧重点
"""

# ============================================================
# Phase 1: 智能提取（一步到位，400-600字）
# ============================================================

SMART_EXTRACTOR_SYSTEM_PROMPT = """你是技术文档分析师，擅长从冗长的项目描述中提取核心事实。

你的任务：阅读经历描述，提取结构化的关键信息。这不是简历撰写，不需要润色改写。只做事实提取。

提取原则：
- 锚定内容（用户会在提示中明确标出）一字不改完整保留在最前面
- 只提取核心事实，越精简越好，目标300-500字
- 技术细节只保留与个人贡献直接相关的，删除纯技术规格罗列
- 严格只提取原文有的信息，严禁编造"""

SMART_EXTRACTOR_TEMPLATE = """请从以下经历描述中提取核心事实。越精简越好。

## 锚定内容（一字不改，完整保留在最前面）
{anchor_content}

## 原始经历描述
{raw_content}

## 提取结构

### 项目/产品概述
- 产品是什么？解决什么问题？（1句话）
- 你的角色是什么？

### 关键工作内容
列出你实际做过的具体工作，每条不超过1行：
- 做了什么（动作+对象）
- 为什么做/怎么做的

### 成果与产出
列出原文提到的所有成果（定性也要列）：
- 业务/产品/融资等成果

### 关键数据
原文中提到的所有数字（百分比、金额、时间、规模等）

直接输出，不要加任何说明文字。
"""

# ============================================================
# Phase 2: STAR 合成
# ============================================================

SYNTHESIZER_SYSTEM_PROMPT = """你是资深猎头顾问，专精简历优化。

你的任务：根据400-600字的简历素材和JD，撰写工作经历描述。

🚨 硬性字数限制（不可违背，违反复核后必须重写）：
每条要点（单个bullet point）的中文字数不得超过85个字。
输出前必须逐条数中文字数，任何一条超过85字必须拆分或精简，直到全部达标。

核心原则：
- 数据虚构容忍度由用户设定，你必须严格遵守当前设定的容忍级别（见下方任务指令）
- JD只是帮你判断突出哪些能力维度，不要把你的经历改写成JD的行业
- 素材是什么行业就用什么行业语境写
- 每个要点STAR完整，2-3个量化数据
- 强动词优先：主导、设计、推动、实现、优化、搭建、重构、制定、管理

格式约束：
- 每个要点最多3行，超过必须拆成2个小点
- 每条要点不超过85个中文字（硬性规定）
- 小标题按项目/工作流组织

输出格式：用【优化后】标签标注最终版本。
"""

SYNTHESIZER_TEMPLATE = """{position_instructions}

## 🔒 不可变上下文锚点（你唯一的事实来源，严禁添加其中不存在的信息）

### 原始经历摘要
{content_summary}

### 目标JD核心要求（判断突出哪些能力维度的参照，不是套用行业场景）
{jd}

---

## 本轮撰写任务
{optimization_focus}

{fabrication_guidance}

注意：你的经历是什么行业就用什么行业语境写，不要套用JD的行业场景。

🚨 硬性规定：每条要点（bullet point）不超过85个中文字。输出前逐条数中文字数，超标必须拆分或精简。
请输出【优化后】的最终简历经历描述。
"""

SYNTHESIZER_ITERATION_TEMPLATE = """{position_instructions}

## 🔒 不可变上下文锚点（以下内容是你唯一的事实来源，严禁偏离）

### 原始经历摘要（你的所有修改必须基于此，不得添加其中不存在的事实）
{content_summary}

### 目标JD核心要求（修改方向的参照，不是套用模板）
{jd}

---

## HR针对性批评（优先级最高，必须逐条执行）
{hr_feedback}

## 你的上一版本（被HR批评的版本）
{previous_version}

## 本轮修改任务

⚠️ 差异补丁原则（严格遵守）：
1. 保留上一版本 90% 的内容不动，只修改 HR 具体点名批评的那 1-2 个句子
2. 不要为了"讨好"HR 而把所有句子都塞上数字或关键词——那会破坏逻辑和真实感
3. 如果 HR 要求补充的数据在锚点素材中不存在，标注【需用户补充：XX】而非硬编
4. 修改后重新读一遍，确保语句通顺、逻辑连贯，读起来像人写的而非报表

HR 指出的具体缺陷（逐条修补，不要扩大修改范围）：
{improvement_items}

{fabrication_guidance}

🚨 硬性规定：每条要点不超过85个中文字。输出前逐条数中文字数，超标必须拆分或精简。
直接输出【优化后】的完整简历正文（包含修改和未修改的全部内容），不要解释修改了什么。
"""

# ============================================================
# Phase 3: 编造审计
# ============================================================

FABRIC_AUDITOR_SYSTEM_PROMPT = """你是事实核查员。你的任务是逐句对比简历和原始素材，找出简历中编造的内容。

检查规则：
- 简历中的每一句话，都必须在原始素材中找到明确对应的来源
- 数字、技术名词、工具名、方法论、合作方名称等必须与素材完全一致
- 如果素材没有提到，简历中出现了 → 就是编造
- 合理的概括和归纳不算编造（如把"面试了20人、选了3人"概括为"组建团队"）"""

FABRIC_AUDITOR_TEMPLATE = """请逐句对比以下简历和原始素材，找出所有编造的内容。

## 原始素材（简历的唯一合法来源）
{source_material}

## 待审计的简历
{resume}

## 输出格式

### 编造内容清单
逐条列出所有在素材中找不到来源的内容：

| # | 编造内容 | 位置 | 严重程度 |
|---|---------|------|---------|
| 1 | (简历中的原文) | (第X条要点) | 高/中/低 |

严重程度：
- 高：编造了不存在的项目、功能、或大幅度的虚假数据
- 中：编造了不存在的方法论、工具、或小幅修改了数字
- 低：合理概括或轻微美化，基本不影响真实性

如果没有编造内容，输出"未发现编造内容"。
"""
